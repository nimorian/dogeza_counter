<%= form_for(@doge) do |f| %>
  <% if @doge.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@doge.errors.count, "error") %> prohibited this doge from being saved:</h2>

      <ul>
      <% @doge.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :count %><br>
    <%= f.number_field :count %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
